{
  "schema_version": "1.0",
  "name": "{{title}}",
  "task_type": "{{task_type}}",
  "canonical_url": "{{canonical_url}}",
  "target_agents": {{target_agents_json}},
  "objective": "{{objective}}",
  "input_contract": {
    "required": {{required_inputs_json}},
    "optional": {{optional_inputs_json}}
  },
  "output_contract": {{output_contract_json}},
  "scoring_rubric": {{scoring_rubric_json}},
  "disclaimer": "Research commentary only. Not investment advice."
}
